// Leggere tutti gli elementi di un array
const names = ['Paolo', 'Francesca', 'Mario', 'Marta', 'Ernesto'];
for(let i = 0; i < names.length; i++) {
    const thisName = names[i];
    console.log(thisName);
}
